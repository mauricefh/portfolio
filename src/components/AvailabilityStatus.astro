---
import personal from "../data/personal.json";

type AvailabilityStatus = "available" | "open" | "unavailable";

const status = personal.availability_status as AvailabilityStatus;

const statusConfig = {
  available: {
    emoji: "🟢",
    label: "Available",
    description: "Seeking remote software development roles",
  },
  open: {
    emoji: "🟡",
    label: "Open",
    description: "Considering opportunities",
  },
  unavailable: {
    emoji: "🔴",
    label: "Unavailable",
    description: "Not currently seeking roles",
  },
};

const config = statusConfig[status];
---

<span
  class="inline-flex items-center gap-2 rounded-lg border border-accent px-3 py-1 text-sm text-accent"
>
  <span>{config.emoji}</span>
  <span>{config.label}</span>
  <span class="text-mono-400">-</span>
  <span class="text-mono-300">{config.description}</span>
</span>
